// @ts-check
import { defineConfig } from "astro/config";

export default defineConfig({
  // Deploying under a subdirectory? Set `base` here AND edit framePath in
  // src/components/frames.js — the frames are fetched at runtime by the engine,
  // which Astro's base does not rewrite.
});
